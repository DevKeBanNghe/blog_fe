const DEFAULT_PAGINATION = {
  page: 1,
  itemPerPage: 4,
};

const SELECT_LIMIT_OPTIONS = 6;

const LOADING_STATUS = {
  IDLE: 'idle',
  PENDING: 'pending',
  SUCCEEDED: 'succeeded',
  FAILED: 'failed',
};

const PREFIX_ADMIN_PAGE = '/admin';

const PERSONAL_BRAND = 'Dev Kể Bạn Nghe';

const ACTIVATE_STATUS = {
  ACTIVE: 1,
  INACTIVE: 0,
};

export { DEFAULT_PAGINATION, SELECT_LIMIT_OPTIONS, LOADING_STATUS, PREFIX_ADMIN_PAGE, PERSONAL_BRAND, ACTIVATE_STATUS };
